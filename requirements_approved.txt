# ============================================================================
# HybridRAG3 -- Approved Requirements
# Updated: 2026-03-02
# Changelog: 2026-03-02 -- Added ocrmypdf* for scanned PDF OCR enhancement workflow
#                          (YELLOW: pending approval/validation on work environment)
# Changelog: 2026-03-01 -- Added pdf2image, pytesseract, colorama (were installed but missing from list)
#                          Moved faiss-cpu to commented BLUE section (not installed, future phase)
#                          Added Tesseract OCR + Poppler system binaries* (required for OCR pipeline)
#                          Added 9 parser deps* (YELLOW: applying for approval) -- enables .doc, .msg,
#                          .dxf, .evtx, .pcap, .psd, .rtf, .stl, .vsdx parsing
#                          Fixed indexer supported_extensions allowlist (was missing 35+ formats)
# Changelog: 2026-02-24 -- Added pytest, psutil; cross-referenced against waiver_cheat_sheet_v4b
# Pin policy: Exact versions from waiver cheat sheet v4b
# openai PINNED to 1.109.1 -- never upgrade to 2.x (breaking API changes)
# Bumped 1.51.2->1.109.1 (2026-02-26): 1.51.2 passes `proxies=` to httpx 0.28 (removed kwarg); 1.109.1 fixes this
# xxhash ELIMINATED -- replaced by built-in hashlib
# LangChain BANNED -- dependency hell
# HuggingFace RETIRED -- embeddings now served by Ollama (nomic-embed-text)
# China-origin models EXCLUDED (Qwen, DeepSeek, BGE-M3)
# Meta Llama models BANNED (Meta AUP prohibits weapons/military use)
# ============================================================================

# -- Core AI / Model APIs --------------------------------------------------
openai==1.109.1                # MIT -- PINNED v1.x forever; never 2.x syntax (latest 1.x, httpx 0.28 compatible)
tiktoken==0.8.0                # MIT -- OpenAI token counting (offline, no network)

# -- Embeddings ------------------------------------------------------------
# Embeddings are now served by Ollama (nomic-embed-text, 768-dim).
# No Python embedding packages needed -- Ollama handles inference via HTTP.
# Removed: sentence-transformers, torch, transformers, tokenizers,
#          huggingface_hub, safetensors, scipy, scikit-learn, sympy,
#          threadpoolctl, einops, joblib, filelock, fsspec, networkx, mpmath
# This eliminates ~2.5GB of dependencies and the AI Use Case approval
# requirement that HuggingFace models trigger.

# -- Numerical -------------------------------------------------------------
numpy==1.26.4                  # BSD-3 -- Last 1.x release (2.x has breaking changes)

# -- Vector Store -----------------------------------------------------------
# faiss-cpu==1.9.0             # MIT -- Vector similarity search (BLUE: not yet installed, apply for next phase)

# -- Data Validation -------------------------------------------------------
pydantic==2.11.1               # MIT -- Data validation (openai SDK dep)
typing-inspection==0.4.2       # MIT -- Pydantic dep
annotated-types==0.7.0         # MIT -- Pydantic dep
typing_extensions==4.15.0      # PSF-2.0 -- Python typing backports (PSF/USA)

# -- Document Parsers ------------------------------------------------------
pdfplumber==0.11.9             # MIT -- Primary PDF text extraction
pdfminer.six==20251230         # MIT -- PDF parsing engine (pdfplumber dep)
pypdf==6.6.2                   # BSD-3 -- PDF metadata and page counting
pypdfium2==5.3.0               # Apache 2.0 -- Fast PDF rendering (Google PDFium engine)
python-docx==1.2.0             # MIT -- Word .docx reader
python-pptx==1.0.2             # MIT -- PowerPoint .pptx reader
openpyxl==3.1.5                # MIT -- Excel .xlsx reader
xlsxwriter==3.2.9              # BSD-2 -- Excel .xlsx writer
lxml==6.0.2                    # BSD-3 -- XML/HTML parsing (Office XML dep)
et_xmlfile==2.0.0              # MIT -- XML utility (openpyxl dep)
pillow==12.1.0                 # HPND -- Image processing
pdf2image==1.17.0              # MIT -- PDF to image conversion (OCR pipeline)
pytesseract==0.3.13            # Apache 2.0 -- OCR bridge for scanned PDFs
ocrmypdf==16.10.4              # MPL-2.0 -- Scanned PDF OCR enhancement (YELLOW*: pending approval/validation)

# -- OCR System Binaries (NOT pip -- separate install required) ---------------
# * These are standalone executables that the Python wrappers above call.
#   They must be installed via system installer, software store, or scoop.
#   Added 2026-03-01 -- required for scanned PDF and image OCR pipeline.
# Tesseract OCR 5.x             # Apache 2.0 -- Google-maintained OCR engine (APPROVED*)
#   Publisher: Google / tesseract-ocr (github.com/tesseract-ocr/tesseract)
#   Install: software store "Tesseract OCR" or UB-Mannheim Windows builds
#   Pinned: pytesseract==0.3.13 (Python wrapper already approved above)
# Poppler 26.x                  # GPL-2.0 -- PDF rendering library (APPLYING*)
#   Publisher: freedesktop.org (poppler.freedesktop.org)
#   Install: software store "poppler-utils" or scoop install poppler
#   Note: GPL-2.0 -- used as standalone tool only (no linking), same engine as Chrome/LibreOffice
#   Pinned: pdf2image==1.17.0 (Python wrapper already approved above)

# -- HTTP / Networking ------------------------------------------------------
httpx==0.28.1                  # BSD-3 -- Async HTTP client (openai SDK dep + embedder)
requests==2.32.5               # Apache 2.0 -- HTTP client (pip dep, likely pre-installed)
urllib3==2.6.3                  # MIT -- HTTP internals (requests dep, likely pre-installed)
sniffio==1.3.1                 # MIT -- Async detection (httpx dep)
cryptography==44.0.2           # Apache 2.0 -- AES-256 encryption (pip dep, likely pre-installed)

# -- Web Server / API ---------------------------------------------------------
fastapi==0.115.0               # MIT -- REST API framework (Tiangolo/USA)
uvicorn==0.41.0                # BSD-3 -- ASGI server (Encode/UK)
starlette==0.38.6              # BSD-3 -- ASGI toolkit (fastapi dep, DoS fix in 0.38.2+)
python-multipart==0.0.22       # Apache 2.0 -- Form data parsing (fastapi dep)
click==8.3.1                   # BSD-3 -- CLI toolkit (uvicorn dep, Pallets/USA)

# -- GPU LLM Inference (Workstation Only) ------------------------------------
# vllm==0.10.1                 # Apache 2.0 -- GPU LLM inference (YELLOW: waiver request 2)
#   ^^^ COMMENTED OUT: conflicts with openai==1.109.1 (vllm needs openai>=1.99.1)
#   Not needed for demo -- Ollama handles local GPU inference for now.

# -- Credential Storage -------------------------------------------------------
keyring==23.13.1               # MIT -- Windows Credential Manager access (Python 3.12 compatible)
jaraco.classes==3.4.0          # MIT -- keyring dependency
more-itertools==10.8.0         # MIT -- keyring dependency

# -- Testing / Development (APPLYING FOR APPROVAL) ----------------------------
pytest==9.0.2                  # MIT -- Test framework (YELLOW: applying for approval)
psutil==7.2.2                  # BSD-3 -- Process utilities (YELLOW: applying for approval)

# -- Configuration / Logging ------------------------------------------------
pyyaml==6.0.2                  # MIT -- YAML parsing (ALREADY APPROVED in store)
structlog==24.4.0              # MIT -- Structured JSON logging for audit trails
rich==13.9.4                   # MIT -- Terminal formatting (display-only)
tqdm==4.67.3                   # MIT -- Progress bars (display-only)
regex==2026.1.15               # Apache 2.0 -- Enhanced regex (text processing)
colorama==0.4.6                # BSD-3 -- Console color output (Windows terminal support)

# -- Extended Parser Dependencies (YELLOW*: applying for approval 2026-03-01) --
# These enable the specialty parsers in src/parsers/. Without them, files with
# these extensions silently return empty text and are skipped as "no text extracted."
# All licenses are permissive (MIT, BSD, Apache 2.0). No GPL. No China-origin.
olefile==0.47                  # BSD -- Legacy Office binary format reader (.doc, .msg)
ezdxf==1.4.3                   # MIT -- AutoCAD DXF reader/writer (engineering drawings)
python-evtx==0.8.1             # Apache 2.0 -- Windows Event Log parser (.evtx, sys admin)
python-oxmsg==0.0.2            # MIT -- Outlook .msg email reader (by python-docx author)
dpkt==1.9.8                    # BSD -- Network packet capture parser (.pcap, cybersecurity)
psd-tools==1.13.1              # MIT -- Photoshop .psd file reader
striprtf==0.0.29               # BSD-3 -- Rich Text Format parser (.rtf documents)
numpy-stl==3.2.0               # BSD -- STL 3D mesh parser (engineering/manufacturing)
vsdx==0.6.1                    # BSD-3 -- Visio .vsdx diagram parser (engineering/logistics)
# Transitive deps (pulled by packages above):
pyparsing==3.3.2               # MIT -- ezdxf dep
fonttools==4.61.1              # MIT -- ezdxf dep
hexdump==3.3                   # MIT -- python-evtx dep
attrs==25.4.0                  # MIT -- psd-tools dep
python-utils==3.9.1            # BSD-3 -- numpy-stl dep
Jinja2==3.1.6                  # BSD-3 -- vsdx dep
MarkupSafe==3.0.3              # BSD-3 -- Jinja2 dep
deprecation==2.1.0             # Apache 2.0 -- vsdx dep
